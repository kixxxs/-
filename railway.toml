[build]
  builder = "nixpacks"
  buildCommand = "echo 'Railway Nixpacks will auto-detect Node.js and run npm install'"

[deploy]
  healthcheckPath = "/api/health"
  restartPolicyMaxRetries = 3

[service]
  name = "artist-manager"

[volume]
  name = "data"
  mountPath = "/data"
